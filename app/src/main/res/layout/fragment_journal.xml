<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="12dp"
    android:background="@drawable/journalbck">

    <ListView
        android:id="@+id/journal_list_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:divider="@null"
        android:dividerHeight="5dp" />

    <TextView
        android:id="@+id/text_journal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="No dreams yet.\nTap + to add one."
        android:textAlignment="center"
        android:textSize="16sp"
        android:textColor="#9d89e0"
        android:layout_gravity="center" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/add_journal_fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="20dp"
        android:backgroundTint="#731fec"
        android:contentDescription="Add journal entry"
        android:src="@android:drawable/ic_input_add" />
</FrameLayout>
