<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#3f3975"
    android:padding="24dp">

    <!-- Title Bar -->
    <TextView
        android:id="@+id/journal_activity_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Capture Your Dream"
        android:textColor="@android:color/white"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="16dp" />

    <!-- Title Input -->
    <EditText
        android:id="@+id/journal_title_input"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Dream title..."
        android:textColorHint="#B0B0B0"
        android:textColor="@android:color/white"
        android:inputType="text"
        android:maxLines="1"
        android:textSize="18sp"
        android:background="@android:color/transparent"
        android:padding="12dp"
        app:layout_constraintTop_toBottomOf="@id/journal_activity_title"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp"/>

    <!-- Content Input (scrollable) -->
    <ScrollView
        android:id="@+id/content_scroll"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"
        android:fillViewport="true"
        app:layout_constraintTop_toBottomOf="@id/journal_title_input"
        app:layout_constraintBottom_toTopOf="@id/button_container"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <EditText
            android:id="@+id/journal_content_input"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/rounded_txt_bg"
            android:hint="Write your dream here..."
            android:inputType="textMultiLine"
            android:textColor="@android:color/white"
            android:textColorHint="#B0B0B0"
            android:minLines="15"
            android:gravity="top|start"
            android:padding="16dp"
            android:textSize="16sp" />
    </ScrollView>

    <!-- Button Container -->
    <LinearLayout
        android:id="@+id/button_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="8dp">

        <Button
            android:id="@+id/btnCancel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Cancel"
            android:backgroundTint="#2B2F6E"
            android:textColor="#9d89e0"
            android:layout_marginEnd="16dp"/>

        <Button
            android:id="@+id/btnSave"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Save Dream"
            android:backgroundTint="#2B2F6E"
            android:textColor="#9d89e0" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
